# 2026.261.1228 — docs: correct the skill count to the live 12 entities

## Summary of changes

Corrects a stale skill count in the repo README.

- `README.md`: "It declares **13 `skill:` entities**" → **12**.

**Why 13 was wrong:** at the candy's extraction (`#1`) there were 13 skill entities, including `wl-skill` and `wl-overlay-skill`. PR #4 (2026-08-30) moved those two to the plugins that serve them, leaving 11; PR #13 (2026-09-17) added `jetkvm-skill`, making **12**. The README's count was never updated for either change.

**Closes / relates to:** a documentation follow-up to `#4` and `#13`.

**Breaking change + rollback:** `none` — README prose. Revert the merge to roll back.

## Change class

`documentation-only` (`README.md`).

## How tested

The current entity count, from the tracked `charly.yml`:

```console
$ grep -cE '^    skill:' charly.yml
12
$ grep -oE '^[a-z0-9-]+-skill:' charly.yml | tr -d ':'
adb-skill
android-skill
appium-skill
cdp-skill
check-skill
check-sway-browser-vnc-skill
dbus-skill
libvirt-skill
record-skill
spice-skill
vnc-skill
jetkvm-skill
```

The manifest still validates at the pinned charly:

```console
$ /usr/local/bin/charly-lc-pin box validate
$ echo $?
0
```

## Change classification

- **Change class:** documentation-only.
- **Verification gate:** the class gate (`charly box validate` at the pinned charly) + the count greps above.
- **Attribution tier:** `documentation reviewed`.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:skills`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** root-caused to two unreflected entity changes (#4 removed the wl skills, #13 added jetkvm); fixed at the source. Gate is zero-warning.
- **R2 no out-of-scope:** the one-count correction is the whole change.
- **R3 no duplication:** the README states the count once.
- **R4 no workaround:** the count is corrected, not removed.
- **R5 hard cutover + grep:** the greps above.
- **R6 git safety:** fresh branch off current `origin/main`; no force push, no hooks bypass, no direct push to `main`.
- **R7 runtime gate:** `N/A — README prose`; class gate pasted.
- **R8/R9:** `N/A — no artifact/binary`.
- **R10 disposable + coverage:** `N/A — docs-only`.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*