# 2026.246.1622 — docs(record): document the gif method (agg tool row, prerequisites, when-to-use)

## Summary

Documents the new **`record: gif`** method in the record skill: renders a STOPPED
terminal recording (.cast) to an animated GIF with agg (asciinema/agg, installed by
the asciinema candy alongside a DejaVu Sans Mono font).

- Quick Reference table: new "Render a GIF" row with the full option surface
- Methods section: new `record: gif` subsection (example step + agg option docs)
- Recording Tools table: agg row
- Prerequisites: GIF rendering needs the asciinema layer (agg + font)
- When to Use: GIF conversion triggers

The code changes this documents land in sibling PRs: opencharly/plugin-record#5
(the `record: gif` method) and opencharly/layer-asciinema#3 (the agg binary +
font).

## How tested

`charly box validate` (CI-pinned charly v2026.238.1242) — PASS, no output.

The documented `record: gif` method is proven by the sibling PRs' R10 bed
(opencharly/plugin-record#5): `charly check run check-gif-r10` PASS (13/13 steps),
with `record: gif` rendering a 17567-byte GIF live.

## Rulebook compliance

- **R3 (no duplication):** the skill is the single doc surface for the record verb;
  the gif method is documented once here, not duplicated in the plugin description.
- **R6 (git safety):** PR-only; no force-push; no direct main push.
- **R7 (prove the gate):** box validate output pasted above; the R10 evidence for
  the documented method lives in the sibling plugin-record PR.

## Change classification

- **Change class:** documentation (skill content)
- **Verification gate:** box validate (docs-only change class)
- **Attribution tier:** documentation reviewed

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*