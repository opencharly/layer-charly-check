# 2026.250.1955 — feat(skill): check — check live is VERIFY-ONLY, never an R10 gate

## Summary

Documents the mode discipline in the check skill: `charly check live` skips every mutating `run:` step by design (verify-only — verify_checks.go threads in.VerifyOnly), so a session-instrument R10 proof run via check live produces skipped mutating steps + cascading downstream failures and is NOT evidence. The R10 gate is always `charly check run <bed>`. Measured: 26 skipped mutating steps + 43 downstream failures from the check-live substitution (vs the full check run at 6/7 green).

## How tested

Source-level docs change in the owning candy skill: entity; the marketplace corpus regeneration + drift gate land at the refs-bump PR (same wave).

## Rulebook compliance

- R1: the wrong-mode incident encoded as a hard mode-discipline rule.
- R6: PR-only; no force-push; no direct main push.
- R3: single canonical source.

## Change classification

Class: documentation/skill-source. Gate: repo validate + marketplace regeneration. Attribution: **documentation reviewed**.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*