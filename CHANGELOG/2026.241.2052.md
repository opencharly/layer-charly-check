# 2026.241.2052 — fix(skill): sync the check beds-and-r10 roster with the live tree

## Summary

The `/charly-check:check` skill's `beds-and-r10.md` reference was stale: it claimed the main repo's `charly.yml` owns only `check-k3s-vm`, `check-local`, and `check-charly-vm`, and listed a 12-bed roster. The live tree owns **33 beds** in `charly/charly.yml` (vm/pod/group/local/external mechanism beds) plus **52 distro beds** in the `box/*` submodules. `check-local` no longer exists — it is `check-local-vm` (a VM bed running the `kind: local` path inside the disposable eval VM).

Updates the roster table (all 33 main-repo beds + the box submodule beds), the bed-homes paragraph, and the wall-clock section, and adds the note that the live roster (`grep '^check-.*:' <repo>/charly.yml`) is authoritative — rosters change across cutovers.

## How tested

- `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` → `YAML OK` (the edited skill entity parses).
- Every bed name in the updated table verified against the live roster: `grep '^check-.*:' charly/charly.yml` (33 beds) + `grep '^check-.*:' charly/box/{arch,cachyos,debian,fedora,ubuntu}/charly.yml` (52 beds).
- `check-local` confirmed absent from the live roster (`grep -c '^check-local:' charly/charly.yml` → 0); `check-local-vm` present.

## Rulebook compliance

- R1: doc-vs-reality divergence (stale roster) — RCA'd and fixed in the owning repo.
- R5: the retired `check-local` identifier swept from the skill content.
- Docs-only change class (skill entity content in `charly.yml`).

## Change classification

- **Change class:** documentation (skill entity content)
- **Verification gate:** documentation reviewed
- **Attribution tier:** documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*