# 2026.243.0857 — docs(skills): M3 - dot-free checkbed entity naming in check:check

## Summary

M3 memory-to-skill migration: the dot-free checkbed entity-name lesson (proven when `check-mise-26.04`-style names broke deploy-tree keys in the 2026-08-30 session) lands in the check:check skill's bed-authoring guidance.

Change: `charly.yml` (check skill entity) gains a short "Bed & entity naming — dot-free, always" section: deploy-tree keys treat dots as namespace separators, so every `check-*` entity name must be dot-free (`check-mise-ubuntu-2604`, not `…-26.04`).

## How tested

- YAML parses (`yaml.safe_load` OK); the section present (grep `dot-free` = 1).

## Rulebook compliance

- R3: no duplication (no existing dot-free guidance in the corpus; grep across check-family entities returned 0).
- R5: additive in the owning entity.

## Change classification

- Change class: Documentation-only (candy skill entity — check:check).
- Verification gate: documentation reviewed (YAML parse + grep).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*