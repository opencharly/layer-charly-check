# 2026.268.1545 — docs(task): rewire the layer-charly-check skill corpus to the charly task surface

## Summary

Sweep the Taskfile-era name from the `layer-charly-check` skill's R10 matrix: the
`charly` Go code gate row now reads `task build:binary` → the concrete R9 source
build `scripts/bootstrap-charly.sh` (R9 freshness + `charly version` check on the
worktree binary), rather than the deleted Taskfile target.

This also merges `origin/main` (the intervening `kind:check-roster` roster
wording), resolved so the row keeps main's new roster text AND this rename.

The edit is to the validator's own gate-spec table, so T4 applies: the
**maintainer sign-off is posted as a PR comment**, and the change PRESERVES the
gate (same concrete build command + version check; only the deleted name is
replaced).

## How tested

The claim-keyed sweep returns no stale reference in the edited manifest:

```
$ grep -nE '\btask (build:binary|build:install-portable|mods:tidy|cue:gen|wire:gen|proto:gen)\b|Taskfile|taskfiles/' charly.yml
(empty)
```

The edited manifest still parses:

```
$ yq -e '.' charly.yml >/dev/null
(exit 0)
```

## Rulebook compliance

- **R0:** dispatcher skills loaded (git-workflow, check, cutover-policy, skills).
- **R1:** N/A — no anomaly.
- **R2:** N/A — single in-scope edit; no sibling residual (grep above).
- **R3:** N/A — no duplication introduced.
- **R4:** N/A — no workaround; the concrete R9 entrypoint is the sanctioned mechanism.
- **R4a:** the row names the R9 mechanism, and the one repo-relative path
  (`bin/charly`) is removed; the remaining `scripts/bootstrap-charly.sh` is
  charly R9's own sanctioned bare entrypoint (a charly task build cannot exist),
  in a contributor/validator runbook.
- **R5:** the removed name is swept.
- **R6:** N/A — no destructive git action; pushed via the PR branch.
- **R8:** N/A — no emitted artifact.
- **R9:** the row's freshness check is preserved against the worktree binary.
- **R10:** N/A — no disposable bed exercises skill prose (documentation-class).
- **T1–T4:** T4 applies (validator gate-spec edit) — disclosed, justified,
  maintainer-signed-off, gate preserved.

## Change classification

- **Change class:** skill-source config (`charly.yml`) instruction text that names
  the validator's build command — **not** documentation-only (the file is a
  `.yml` manifest and the line is an executable command). Per the reviewer's own
  finding on the class.
- **Verification gate:** claim-keyed sweep empty + YAML parses; the named build
  command is the live `scripts/bootstrap-charly.sh` (proven to build in the
  sibling internals-extra PR).
- **Attribution tier:** `analysed on a live system`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*