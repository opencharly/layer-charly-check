# 2026.246.0617 — docs(skill): record — author record: steps with context: [runtime], not [deploy]

## Summary

Fix stale guidance in the `record` skill: it told authors to author `record:` steps with `context: [deploy]`. Empirically (eval-omarchy pod beds, 2026-09-03):

- `context: [deploy]` → the steps are **SKIPPED** in live mode (`context [deploy] not active in live mode`).
- no context → the steps run in the **box phase**, where the plugin is unavailable (`no provider registered for plugin verb "record"`).
- `context: [runtime]` → the steps run in the **live phase**, where the plugin resolves (the plugin's own `record-verb-dispatches` check uses `[runtime]` and passes).

This change corrects the guidance and all `record:` step examples to `context: [runtime]`. The marketplace corpus regeneration (opencharly/marketplace) is the follow-up that republishes the skill.

## How tested

`charly box validate`: 1 candy, 7 distros, 0 warnings, 0 errors. The corrected authoring was proven live on eval-omarchy's `check-omarchy-suite-pod-pr9912`: with `context: [runtime]` the `record:` steps run in the live phase and the plugin resolves (`record-verb-dispatches` PASS, "No active recordings").

```
$ git -C layer-charly-check show --stat HEAD | head -6
[feat/record-skill-runtime-context f628d06] docs(skill): record — author record: steps with context: [runtime], not [deploy]
 1 file changed, 17 insertions(+), 17 deletions(-)
 charly.yml | 34 +++++++++++++++++-----------------
```

## Rulebook compliance

- **Failure analysis:** the stale guidance was found by a live bed run (R1 — documentation divergence); the empirical evidence (three context variants) is recorded above.
- **No deferral:** N/A — no surfaced issue deferred; the fix lands in this cutover.
- **No duplication:** the guidance is corrected once in the skill source.
- **No workarounds:** the guidance is fixed, not worked around.
- **Delete legacy completely:** the `context: [deploy]` guidance and examples are replaced.
- **Git safety:** working tree checked before commit; no force-push, no direct push to main; feature branch + pull-request-only landing.
- **Prove the gate:** `charly box validate` passed on the final tree.
- **Fresh disposable proof:** N/A — documentation-only change (skill entity prose).

## Change classification

- **Change class:** documentation (skill entity prose; no pins/scripts/runtime paths)
- **How verified:** documentation reviewed
- **Confidence:** documentation reviewed

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*