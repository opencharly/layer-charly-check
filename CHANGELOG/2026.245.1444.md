# 2026.245.1444 — docs(skills): record lanes — record: run, record_env, spice: record

## Summary

Documents the recording lane capabilities in the check-family skill sources (the marketplace corpus is GENERATED from these entities):

- **record skill:** the `record: run` method (send a command + wait for its output to settle, `settle_ms` overridable) and the `record_env` modifier (VM/desktop venues: the compositor session env — XDG_RUNTIME_DIR/WAYLAND_DISPLAY — overrides the container-shaped defaults so wf-recorder attaches to the real display).
- **spice skill:** the `spice: record` method (host-side MJPEG video capture of the VM display; start/stop + fps + artifact + validators; documented limits: server streams/GLZ not decoded, audio stubbed) with an authored example incl. the drive step and the copy-before-teardown note.

All three capabilities are merged charly surfaces: plugin-record #3 (record_env) + #4 (record: run), plugin-spice #4 (spice: record).

## How tested

Docs-only (skill-entity content; the generated corpus regeneration + drift gate is the marketplace repo's deploy workflow — this PR's tag bump follows in the marketplace refs list). The documented behaviors are R10-proven charly surfaces (plugin-record#3/#4 + plugin-spice#4 merged with pasted bed evidence).

## Rulebook compliance

- R1: docs via PR to the owning repo (layer-charly-check). R3: no duplication — the corpus is GENERATED from these entities. No pins changed.

## Change classification

- **Change class:** docs (skill sources)
- **Verification gate:** entity YAML conforms to the skill schema; corpus regen is the marketplace deploy gate
- **Attribution tier:** documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*