# 2026.261.0617 — docs(skill): cover running charly on the JetKVM appliance

## Summary of changes

Extends the `jetkvm` skill entity with the **appliance-resident charly** story.

- `charly.yml` (`jetkvm-skill`): the `description` and `triggers` gain the on-device case, and the skill body gains a **"Running charly ON the device"** section covering the `opencharly/charly-jetkvm` tool (`install`/`verify`/`status`/`uninstall`), why delivery is host-side-over-ssh (the device's `wget` does not validate TLS), and **why there is deliberately no `charly mcp serve` on the device** (the ~199 MB / 1-core appliance OOM-hung when the MCP server forked a full CLI model; KVM control stays the host-side `jetkvm:` verb).

**Closes / relates to:** documents `opencharly/charly-jetkvm` (new repo) and the OOM finding from its development.

**Breaking change + rollback:** `none` — a skill-content extension. Revert the merge to roll back.

## Change class

`documentation-only` — a skill entity's prose + triggers in `charly.yml`; no code, no pin, no candy behavior.

## How tested

The change is a YAML block-scalar edit to one skill entity. The non-runtime standards apply:

```console
$ # the manifest still parses as YAML after the block-scalar edit
$ python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"   # (parsed with a YAML loader)
YAML OK charly.yml

$ # the new section is present exactly once, under jetkvm-skill
$ grep -c "Running charly ON the device" charly.yml
1
$ grep -c "charly-jetkvm install --host" charly.yml
1
```

The claims in the section are the mechanism the shipped tool implements: `charly-jetkvm` downloads release assets host-side with `gh` and streams them over ssh (verified end-to-end against a container bed and a live armv7 JetKVM, reported in `opencharly/charly-jetkvm`), and the no-MCP decision is the measured OOM from that work.

## Change classification

- **Change class:** documentation-only (`charly.yml` skill content).
- **Verification gate:** the non-runtime standards — YAML parse + presence greps above, plus a review of each claim against the tool that implements it.
- **Attribution tier:** `documentation reviewed` — the whole diff is skill prose, validated by the parse + greps and a review of the claims.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:skills` (doc/skill conventions), `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the trigger was the on-device OOM (root-caused to `charly mcp serve` forking a full CLI model on a 199 MB device); the section records the finding rather than glossing it. No build/test warnings (no code).
- **R2 no out-of-scope:** the skill extension is the whole change.
- **R3 no duplication:** the on-device section is the single place the appliance story lives; it cross-references the verb sections rather than restating them.
- **R4 no workaround:** the doc states the real delivery mechanism (host-side `gh` + ssh) and the real constraint (no on-device MCP), not a softened caveat.
- **R4a product-first docs:** the section names the `charly-jetkvm` tool's subcommands as product surface, not a repo-relative manual build.
- **R5 hard cutover + grep:** executed and pasted above.
- **R6 git safety:** fresh branch off current `origin/main`; status inspected; no force update, no hooks bypass, no direct push to the default branch.
- **R7 runtime gate:** `N/A — documentation-only change class`.
- **R8 artifact invariants:** `N/A — no generated artifact`; the skill corpus is regenerated from this entity by `charly marketplace generate` (a separate marketplace PR).
- **R9 binary == source + deps:** `N/A — no binary or dependency change`.
- **R10 disposable + coverage:** `N/A — docs-only`; the non-runtime standards are the gate.
- **RDD / ADE / SDD:** `N/A — no candy, plan:, or schema touched`.
- **Kernel/plugin boundary law:** `N/A — no code`.
- **Disposable-only autonomy:** `N/A — nothing destroyed or rebuilt`.
- **Clean architecture + Go gates:** `N/A — no Go change`.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; the tag-on-merge workflow generates the entry from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*