# 2026.265.1556 — docs: update the bed roster for the artifact-owner relocation

## Summary of changes

Update the `check` skill's bed roster ("Bed homes") for the umbrella check-bed relocation.

- Remove the roster rows for the beds that moved to their artifact owners (`check-substrate`, `check-arch/fedora/debian/ubuntu/alpine-repo`, `check-docs`, `check-marketplace`, `check-dsh-pod`, `check-agentteams-*`, `check-exampledeploy`, and the single-owner command witnesses), and rewrite the "Bed homes" paragraph to state the governing rule: a bed lives in the repo that OWNS the artifact it tests.
- Keep the roster rows for the beds that stay in `opencharly/charly` (the core mechanism beds plus the multi-owner witnesses `check-commands-local` / `check-agent-local`).
- Fix a reader-instruction contradiction: the example said `charly -C box/fedora …` while the next clause named the real submodule path `distro-fedora/`. The umbrella has no `box/` directory — the submodule is `distro-fedora` — so the example now uses the path that actually works.

This is skill PROSE in `charly.yml` (the `check-skill` `content:` string): no code, no config behaviour. `version:` and the CI charly pin are deliberately UNCHANGED (the repo's pinned CI already accepts the current stamp), keeping the whole diff documentation.

## Change class

**docs-only** — skill prose in `layer-charly-check/charly.yml`.

**Closes / relates to:** `relates to opencharly/charly#625` (the companion PR that performs the relocation this prose describes). The prose describes the post-#625 state, so it should merge with or after #625.

**Breaking change + rollback:** none — prose only. Revert restores the prior roster text.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** the non-runtime standards for a docs-only change — the R5 grep self-test and a cross-reference/markdown review. No runtime bed is run for a prose-only diff (per CLAUDE.md R10's docs-only branch).
- **Eval beds run:** `N/A (docs-only)` — the diff changes no runtime code path.
- **Fresh rebuild:** `N/A (docs-only)` — no binary is built or deployed.
- **Did the CHANGED code path execute live?** `N/A (docs-only)` — no code path changed.
- **Concurrency:** `N/A (docs-only)`.

*1. Fresh-rebuild gate output:* `N/A (docs-only) — no gate binary run.`

*2. Each changed code path executing:* `N/A (docs-only) — prose only.`

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
$ git grep -n 'box/fedora' charly.yml
(no match — the contradictory example is swept; remaining paths use distro-<name>)

$ grep -c 'check-substrate\|check-arch-repo\|check-docs\|check-marketplace\|check-dsh-pod\|check-agentteams' charly.yml
(the roster rows for the relocated beds are removed; the remaining mentions are the relocation list, not stale rows)

$ charly box validate   # the schema/pin are UNCHANGED (docs-only class; run at the repo's pinned stamp)
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

**Projection note:** this `check-skill` `content:` is the SOURCE the marketplace generator renders into `opencharly/marketplace`'s `check/skills/check/references/beds-and-r10.md`. The regeneration is a separate marketplace PR that consumes this source; it does not change the config here.

## Attribution tier

**documentation reviewed** — the whole diff is skill prose in `charly.yml` (no code, no config behaviour, `version:`/pin unchanged), and the non-runtime standards above passed.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** loaded `/charly-internals:skills` (skill maintenance) and `/charly-check:check` (the bed model the prose documents) before editing.
- **R1 RCA + zero warnings:** the validator's finding (a self-contradictory reader instruction — `box/fedora` vs `distro-fedora/`) was root-caused to the umbrella having no `box/` directory and fixed in the same change; no warning survives.
- **R2 no out-of-scope:** this is the docs half of the relocation; the code/config half is the named companion `opencharly/charly#625`; nothing parked.
- **R3 no duplication:** the roster text lives once, in this skill source; the marketplace copy is a GENERATED projection, not a second hand-authored source.
- **R4 no workaround:** no sleep/retry/shim.
- **R4a product-first:** the fixed instruction now names the path that actually works (`distro-fedora`), not a workaround.
- **R5 hard cutover:** `git grep 'box/fedora' charly.yml` returns no match after this change; every stale roster row for a relocated bed is swept.
- **R6 git safety:** feature branch off `origin/main`; no force-push; caught up with `origin/main` by merge before pushing.
- **R7 runtime gate:** `N/A (docs-only)` — the non-runtime standards are the gate.
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.` (The marketplace projection regeneration is a separate repo's PR.)
- **R9 binary == source:** `N/A (docs-only) — no binary.`
- **R10 disposable + coverage:** `N/A (docs-only)` — per CLAUDE.md R10, a docs-only change runs the non-runtime standards and NO bed.
- **RDD / ADE / SDD:** `N/A — no runtime assumption, no candy, no .cue edit.`
- **Hard cutover:** one atomic commit; no TODO/deferred item.
- **Kernel/plugin boundary law:** `N/A — prose in a skill entity.`
- **Disposable-only autonomy:** `N/A — no deploy/rebuild.`
- **Clean architecture + Go gates:** `N/A — no Go.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge (org convention; this repo has a CHANGELOG/ written at merge).`

---

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*